{
  "installCommand": "npm install",
  "buildCommand": "npm run --workspace client build && node -e \"const fs=require('fs');fs.rmSync('dist',{recursive:true,force:true});fs.cpSync('client/dist','dist',{recursive:true});\"",
  "outputDirectory": "dist"
}
